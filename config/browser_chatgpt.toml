# Lower-level browser transport profile for manual or supervisor-owned external-agent loops.
[workflow]
mode = "semi_strict"
enable_literature = true
max_branches = 2
max_prover_cycles = 3
max_total_tokens = 2000000
max_tokens_per_branch = 1000000
max_total_calls = 20000
max_calls_per_branch = 5000
run_root = "runs"
prompts_root = "prompts/api"
provider_timeout_seconds = 60

[role_runtime.formalizer]
provider = "external_agent"
model = "chatgpt-5.4-pro-extended"
temperature = 0.0
max_output_tokens = 32000
reasoning_effort = "high"

[role_runtime.literature]
provider = "external_agent"
model = "chatgpt-5.4-pro-extended"
temperature = 0.0
max_output_tokens = 64000
reasoning_effort = "high"

[role_runtime.searcher]
provider = "external_agent"
model = "chatgpt-5.4-pro-extended"
temperature = 0.0
max_output_tokens = 32000
reasoning_effort = "high"

[role_runtime.breakdown]
provider = "external_agent"
model = "chatgpt-5.4-pro-extended"
temperature = 0.0
max_output_tokens = 32000
reasoning_effort = "high"

[role_runtime.prover]
provider = "external_agent"
model = "chatgpt-5.4-pro-extended"
temperature = 0.0
max_output_tokens = 32000
reasoning_effort = "high"

[role_runtime.reviewer]
provider = "external_agent"
model = "chatgpt-5.4-pro-extended"
temperature = 0.0
max_output_tokens = 32000
reasoning_effort = "high"

[role_runtime.consolidator]
provider = "external_agent"
model = "chatgpt-5.4-pro-extended"
temperature = 0.0
max_output_tokens = 32000
reasoning_effort = "high"
